# Open questions — snv_kallvatten

## Two subtraction conflicts

`B313.KV1_VMM22_V` and `B315.KV1_VMM21_V` each appear as − term in two
different rows. Ontology can only attach each to one parent; the
longer row (pool) loses. Expect small residuals on the pool building:
B310 (row 26) loses B313.VMM22 subtraction; B311 (row 27, the 16-term
pool) loses B315.VMM21 subtraction.

Impact to monitor after validation; classify as `excel_bug` or
`ontology_drift` per magnitude.
